# TurboQuant MI300X Benchmarking — Python Dependencies
#
# Install order matters. ROCm PyTorch must be installed first.
#
# Step 1: PyTorch ROCm 6.2 (compatible with ROCm 7.2 on gfx942)
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.2
#
# Step 2: Other dependencies
#   pip install -r requirements.txt

# Core ML / inference
transformers>=4.40.0
accelerate>=0.29.0
tokenizers>=0.19.0

# Numerical / scientific
numpy>=1.26.0
scipy>=1.13.0

# Benchmarking & data
pandas>=2.2.0
tqdm>=4.66.0

# Visualization (Phase 5)
matplotlib>=3.8.0
seaborn>=0.13.0

# Triton (ROCm-compatible, usually pre-installed in ROCm images)
# triton  # installed separately — ROCm version needed

# Optional: for model downloads
huggingface-hub>=0.22.0
python-dotenv>=1.0.0
datasets>=2.19.0  # for WikiText-103 perplexity evaluation

# Optional: flash attention (for baseline comparison)
# flash-attn  # ROCm build, install from source if needed

# Optional: FlashInfer (ROCm 0.5.3+, gfx942 support)
# flashinfer  # install from AMD ROCm wheel if available
